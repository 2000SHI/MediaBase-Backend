<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.media_base.mapper.PersonMapper">
    <select id="search" resultType="Person">
        SELECT * FROM person
        <where>
            <if test="keyword != null and keyword != ''">
                name LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
    </select>

    <select id="getMedia" resultType="PersonRole">
        SELECT media_id, media.title, role FROM media_person
        LEFT JOIN media on media_person.media_id = media.id
        WHERE person_id = #{id};
    </select>
</mapper>